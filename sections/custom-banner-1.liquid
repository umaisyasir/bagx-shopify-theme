<style>
  /* Main section layout */
  .banner-section {
    display: flex;
    width: 100%;
    gap: 15px; /* Adjust the gap between columns */
  }

  /* Left column styling */
  .banner-column-1 {
    flex: 0 0 50%; /* Left column is 50% */
    max-width: 50%;
  }

  .banner-column-1 img {
    width: 100%;
    height: auto;
    border-radius: 5px; /* Optional: adds rounded corners */
    transition: transform 0.3s ease, opacity 0.3s ease, box-shadow 0.3s ease; /* Smooth transition */
  }

  /* Hover effects */
  .banner-column-1 img:hover {
    transform: scale(1.05); /* Zoom effect */
    opacity: 0.9; /* Slight opacity change */
    box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15); /* Shadow effect */
  }

  /* Right column styling */
  .banner-column-2 {
    flex: 0 0 50%; /* Right column is 50% */
    display: flex;
    flex-direction: column;
    gap: 15px;
  }

  /* Large banner in right column */
  .large-banner {
    width: 100%;
    margin-bottom: 10px;
  }

  .large-banner img {
    width: 100%;
    height: auto;
    border-radius: 5px; /* Optional: adds rounded corners */
  }

  /* Small banners below the large banner */
  .small-banners {
    display: flex;
    gap: 15px;
  }

  .small-banner {
    flex: 1; /* Each small banner takes up equal space */
  }

  .small-banner img {
    width: 100%;
    height: auto; /* Maintain aspect ratio */
    border-radius: 5px; /* Optional: adds rounded corners */
  }

  /* Responsive behavior */
 @media (min-width: 769px) {
    .banner-section {
      display: none;
    }
  }
</style>

<div class="banner-section">
  <!-- Left Column -->
  <div class="banner-column-1">
    <div class="image-banner">
      <a href="{{ section.settings.link_1 }}">
        <img src="{{ section.settings.image_1 | img_url: 'master' }}" alt="Left Column Banner">
      </a>
    </div>
  </div>

  <!-- Right Column -->
  <div class="banner-column-2">
    <!-- Top Large Banner -->
    <div class="large-banner">
      <a href="{{ section.settings.link_2 }}">
        <img src="{{ section.settings.banner_1 | img_url: 'master' }}" alt="Right Top Banner">
      </a>
    </div>

    <!-- Two Smaller Banners -->
    <div class="small-banners">
      <div class="small-banner">
        <a href="{{ section.settings.link_3 }}">
          <img src="{{ section.settings.banner_2 | img_url: 'master' }}" alt="Right Bottom Left Banner">
        </a>
      </div>
      <div class="small-banner">
        <a href="{{ section.settings.link_4 }}">
          <img src="{{ section.settings.banner_3 | img_url: 'master' }}" alt="Right Bottom Right Banner">
        </a>
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Custom Banners Section aj",
  "settings": [
    {
      "type": "image_picker",
      "id": "image_1",
      "label": "Main Image (Left Column)"
    },
    {
      "type": "url",
      "id": "link_1",
      "label": "Link for Main Image (Left Column)"
    },
    {
      "type": "image_picker",
      "id": "banner_1",
      "label": "Large Banner (Right Column Top)"
    },
    {
      "type": "url",
      "id": "link_2",
      "label": "Link for Large Banner (Right Column Top)"
    },
    {
      "type": "image_picker",
      "id": "banner_2",
      "label": "Small Banner 1 (Right Column Bottom Left)"
    },
    {
      "type": "url",
      "id": "link_3",
      "label": "Link for Small Banner 1 (Right Column Bottom Left)"
    },
    {
      "type": "image_picker",
      "id": "banner_3",
      "label": "Small Banner 2 (Right Column Bottom Right)"
    },
    {
      "type": "url",
      "id": "link_4",
      "label": "Link for Small Banner 2 (Right Column Bottom Right)"
    }
  ],
  "presets": [
    {
      "name": "Custom Banners-theaj",
      "category": "Custom"
    }
  ]
}
{% endschema %}
